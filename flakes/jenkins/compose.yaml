services:
  mcp-jenkins:
    image: ghcr.io/mcp-flakes/jenkins:latest
    build:
      context: .
      dockerfile: Dockerfile
    stdin_open: true
    environment:
      - JENKINS_URL=${JENKINS_URL}
      - JENKINS_USERNAME=${JENKINS_USERNAME}
      - JENKINS_API_TOKEN=${JENKINS_API_TOKEN}
